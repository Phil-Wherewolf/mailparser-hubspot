{
  "name": "mailparser-hubspot",
  "version": "1.0.0",
  "description": "Webhook to sync Mailparser data to HubSpot",
  "private": true,
  "scripts": {
  "build": "echo 'No build needed'",
  "deploy": "vercel --prod"
},
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "vercel": "^44.4.1"
  }
  
}
